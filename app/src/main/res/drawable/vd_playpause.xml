<?xml version="1.0" encoding="utf-8"?>
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24">
    <!-- Base state is pause (two bars). Play is expressed as two quads that
        union exactly to the play triangle, using identical commands
        (M + 3L + Z each) so both directions can path-morph. -->
    <path
        android:name="pp_left"
        android:fillColor="@android:color/white"
        android:pathData="M6,19L6,5L10,5L10,19Z" />
    <path
        android:name="pp_right"
        android:fillColor="@android:color/white"
        android:pathData="M14,19L14,5L18,5L18,19Z" />
</vector>
